# Base URL of this server (Bluesky OAuth uses this for client_id and redirect_uri).
# For local dev: http://localhost:3000
# For production: https://your-domain.com
BASE_URL=http://localhost:3000

# ES256 private key for OAuth client authentication (confidential client).
# Generate at https://jwkset.com/generate: Key type ECDSA, algorithm ES256, use Signature.
# Paste the full JWK JSON string here (including the "d" property). Do not commit the real key.
BLUESKY_OAUTH_PRIVATE_KEY={"kty":"EC","crv":"P-256","d":"...","x":"...","y":"..."}
